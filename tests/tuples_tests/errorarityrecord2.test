NAME: errorarityrecord2
DESCRIPTION: checks that the compiler raises an error when is used a record with the wrong arity
STATUS:CT error
SRC:
(record point x y z)
(point-x (point 1 2))
EXPECTED: 
arity is not correct for record point